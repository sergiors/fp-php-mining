{
    "repositories": [
        {
            "type": "vcs",
            "url":  "git@github.com:sergiors/prelude.git"
        }
    ],
    "require": {
        "prelude/prelude": "dev-master"
    }
}
